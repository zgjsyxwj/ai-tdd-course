# [功能名称] 需求规格说明

> 版本：v1.0 | 日期：YYYY-MM-DD | 状态：草稿/已审查/已批准

## 1. 概述

### 1.1 业务背景

[为什么要做这个功能？解决什么问题？]

### 1.2 目标

[这个功能要达成什么结果？]

### 1.3 范围

- **包含**：[本次实现的内容]
- **不包含**：[明确排除的内容，避免范围蔓延]

---

## 2. 实体模型

### 2.1 [实体名称]

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | int | PK, 自增 | 主键 |
| name | varchar(50) | 必填, 唯一 | 名称 |
| ... | ... | ... | ... |

> 如有多个实体，为每个实体创建一个子节（2.2, 2.3...），并说明实体间关系。

---

## 3. API 规格

### 3.1 POST /api/xxx — [功能描述]

**Request Body:**

| 字段 | 类型 | 必填 | 校验规则 | 说明 |
|------|------|------|----------|------|
| name | string | 是 | 长度 1-50 | 名称 |
| ... | ... | ... | ... | ... |

**Success Response:** `201 Created`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 新创建的资源 ID |
| name | string | 名称 |

**Error Responses:**

| 状态码 | 触发条件 | 说明 |
|--------|----------|------|
| 400 | name 为空 | 参数校验失败 |
| 409 | name 已存在 | 资源冲突 |

---

### 3.2 GET /api/xxx/{id} — [功能描述]

**Path Parameters:**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | int | 资源 ID |

**Success Response:** `200 OK`

| 字段 | 类型 | 说明 |
|------|------|------|

**Error Responses:**

| 状态码 | 触发条件 | 说明 |
|--------|----------|------|
| 404 | 资源不存在 | |

---

> 为每个 API 端点创建一个子节（3.3, 3.4...），保持格式一致。

---

## 4. 业务规则

| 编号 | 规则 | 说明 |
|------|------|------|
| BR1 | [规则描述] | [补充说明/示例] |
| BR2 | [规则描述] | [补充说明/示例] |

> 业务规则是跨 API 的逻辑约束，如："删除用户时，关联的订单也需要软删除"。
> 单个 API 的校验规则写在 API 规格中，不要重复写在这里。

---

## 5. 术语表（可选）

| 术语 | 定义 |
|------|------|
| [术语] | [定义] |
